apiVersion: v1
kind: Service
metadata:
  name: blockmart-backend-svc
spec:
  selector:
    app: blockmart-backend
  ports:
    - protocol: TCP
      port: {{ .Values.backend.port }}
      targetPort: {{ .Values.backend.port }}